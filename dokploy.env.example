# Dokploy Environment Variables Example
# Copy this to your Dokploy application environment

# --- SERVER CONFIGURATION ---
SIMPLEAUTH_LISTEN=:8080
SIMPLEAUTH_VERBOSE=true

# --- USER AUTHENTICATION (REQUIRED) ---
# Format: user1:password1,user2:password2,user3:password3
# Use actual passwords - Simpleauth will hash them automatically
SIMPLEAUTH_USERS=admin:changeThisPassword123,user:testPassword456

# --- TOKEN CONFIGURATION ---
# How long authentication cookies remain valid
# Examples: 24h, 7d, 168h, 30d
SIMPLEAUTH_LIFESPAN=168h

# --- FILE LOCATIONS (Optional, for advanced use) ---
# These have sensible defaults and typically don't need to be changed
# SIMPLEAUTH_SECRET_FILE=/data/simpleauth.key
# SIMPLEAUTH_PASSWORD_FILE=/run/secrets/passwd
# SIMPLEAUTH_HTML_PATH=/web

# --- PRODUCTION EXAMPLES ---
# For production with stronger security:
# SIMPLEAUTH_USERS=admin:SuperSecurePass789!@,reader:ReaderPass123$
# SIMPLEAUTH_LIFESPAN=24h
# SIMPLEAUTH_VERBOSE=false